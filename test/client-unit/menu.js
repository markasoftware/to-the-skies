'use strict';

const lib = require('../lib/lib.js');
const urls = require('../lib/urls.js');
const xhrMock = require('xhr-mock');

describe('left menu', () => {
    global.location = {};
    let view;
    let ctrl;
});
