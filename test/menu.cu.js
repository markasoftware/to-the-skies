'use strict';

const lib = require('./lib.js');
const urls = require('./urls.js');
const xhrMock = require('xhr-mock');

describe('left menu', () => {
    global.location = {};
    let view;
    let ctrl;
});
