'use strict';

const m = require('mithril');

module.exports = () =>
    m('div#description', [
        m('h1#logo', 'To The Skies'),
        m('span', 'Interactive fiction anybody can add to'),
    ]);
